{% extends '_layout.html.twig' %}

{% block title %}Loire Et Orléans | Documents | Ajout{% endblock %}
 {% block stylesheets %}
     {{ encore_entry_link_tags('addDocument') }}
 {% endblock %}
{% block body %}
    <section class="d-md-flex align-items-center mb-5">
        <div class="form w-100">
            <h1 class="my-4 text-center">Ajouter un document</h1>

            {{ include('document/_form.html.twig') }}
            <div class="d-flex justify-content-center">
                <a class="btn btn-lg btn-primary my-4" href="{{ path('document_index') }}">Retour aux documents</a>
            </div>

        </div>

    </section>
{% endblock %}
